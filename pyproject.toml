[project]
name = "jules"
version = "0.1.0"
description = "MCP server for Jules — memory and research capabilities"
dependencies = [
    "fastmcp",
    "requests",
    "openai",
    "rank-bm25"
]

[tool.setuptools]
py-modules = ["server", "omnitool"]

[project.scripts]
jules = "server:main"
